import {
    BrowserRouter,
    // Route,
    Routes
  } from "react-router-dom";

export const AppRoutes = () => {
    return (
      <BrowserRouter>
        <Routes>
          {/* <Route path='/' element={} />
          <Route path='/pokemon' element={} /> */}
        </Routes>
      </BrowserRouter>
    )
  }